import { VdatefilterPipe } from './vdatefilter.pipe';

describe('VdatefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VdatefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
