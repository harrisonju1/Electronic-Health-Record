import { DocidfilterPipe } from './docidfilter.pipe';

describe('DocidfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DocidfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
